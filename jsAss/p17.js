//26.Write a function to find the area and perimeter of a Circle
function parameter(radius)
{
    return(2*Math.PI*radius);
}
function area(r)
{
    return(Math.PI*r*r);
}
console.log(parameter(4));
console.log(area(2));
